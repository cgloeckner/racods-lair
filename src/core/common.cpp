#include <core/common.hpp>

namespace core {

float const MAX_FRAMETIME_MS = 25.f;

// minimum distances that is stepped during one frame
// this is used by the movement and collision system
float const MAX_TILE_STEP = 0.5f;

}  // ::core
